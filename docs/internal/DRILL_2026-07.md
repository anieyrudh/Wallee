# Seeded-violation drill — 2026-07 (Phase 5 exit ritual)

Run with `python scripts/seeded_violation_drill.py` at the Phase-5 exit.
Each seeded violation was applied in a disposable worktree and had to fail
on its **named** check. A guard that has never fired is a claim, not a
control.

| # | Seeded violation | Named check | Result |
|---|---|---|---|
| 1 | 801-line core file | `repo-contract` (check_file_sizes) | FIRED |
| 2 | `printer_1` in a clean core module (safety.py) | `repo-contract` (check_core_purity) | FIRED |
| 3 | Undefined name (F821) | `gates` (ruff) | FIRED |
| 4 | Laptop path in a test | `gates` (check_forbidden_patterns) | FIRED |
| 5 | Private LAN IP in docs | `gates` (check_forbidden_patterns) | FIRED |
| 6 | Deleted `test_safety_invariants.py` | `safety-invariants` (check_contract_manifest) | FIRED |
| 7 | Hand-edited golden trace | `tests` (test_golden_traces) | FIRED |

**Verdict: DRILL PASSED — 7/7 guards fired.**

Note for the record: the first run of violation 7 did **not** fire — the
seeded edit targeted `"NO_ACTION"` with quotes, but the golden stores the
plan as an escaped JSON string, so the mutation matched nothing and the
guard was never actually exercised. The drill caught its own bad seeding,
which is precisely the failure mode this ritual exists to expose. The
seeding was fixed (bare-token edit) and the guard verified to fire.

Repeat the drill after any change to the check scripts, and record each run
here. When run as real PRs against a protected `main`, record the run URLs
instead.
